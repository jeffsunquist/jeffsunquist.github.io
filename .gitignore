# Build output of scripts/deploy.sh (deployed to GitHub Pages, not committed here)
dist/
node_modules/
.slidev/

# Transient per-grade deck variants generated by scripts/deploy.sh
.*.g[67].md

# Agent instructions (kept locally, not committed)
AGENTS.md
